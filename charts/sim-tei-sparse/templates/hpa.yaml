{{- if and (hasKey .Values "global") (hasKey .Values.global.control "simTeiSparse") (.Values.global.control.simTeiSparse.enabled) (.Values.global.control.simTeiSparse.autoscaling.enabled) }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.appName }}-hpa
  labels:
    {{- include "sim-tei-sparse.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.appName }}-deployment
  minReplicas: {{ .Values.global.control.simTeiSparse.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.global.control.simTeiSparse.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.global.control.simTeiSparse.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.global.control.simTeiSparse.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.global.control.simTeiSparse.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.global.control.simTeiSparse.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
